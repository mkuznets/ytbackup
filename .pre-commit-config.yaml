repos:
  - repo: local
    hooks:
      - id: generate
        name: go generate
        description: "Runs `go generate` for all packages"
        entry: go generate ./...
        language: system
        pass_filenames: false
        files: \.(go|py)$
